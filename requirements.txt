# pip install -r requirements.txt

# you should have these packages installed
# apt-get install python3-tk

# run tests:
# run tests with py.test --mpl

matplotlib==1.5.3
pytest==3.0.2
pytest-mpl==0.5
pyqt5
